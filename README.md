# stat_phys
`stat_phys` is a Python library for performing statistical physics simulations.
Currently, the library contains a single module, `canonical`, which provides the `CanonicalMC` class. This class
allows users to create simulations of physical systems within the canonical ensemble. Documentation 
for the library will be uploaded soon.
